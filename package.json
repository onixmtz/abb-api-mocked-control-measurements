{
  "name": "abba-api-mocked-control-measurements",
  "version": "0.0.1",
  "main": "build/debug/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc -p tsconfig.release.json",
    "build": "npm run clean; npm run tsc; cp .env.dist build/release/.env",
    "start": "npm run build; cd build/release; clear; node index.js",
    "tscDebug": "tsc", 
    "buildDebug": "npm run clean; npm run tscDebug", 
    "debug": "npm run buildDebug; cd build/debug; node -r source-map-support/register index.js",
    "clean": "if [ -d \"build\" ]; then rm -rf build; fi"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "source-map-support": "^0.5.16",
    "typescript": "^3.8.2"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
