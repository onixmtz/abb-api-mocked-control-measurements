{
  "name": "abba-api-mocked-control-measurements",
  "version": "0.0.1",
  "main": "build/App.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "build": "PWD=build/ npm run tsc && node build/App.js",
    "start": "PWD=build/ npm run tsc && node build/App.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "typescript": "^3.8.2"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
