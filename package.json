{
  "name": "abba-api-mocked-control-measurements",
  "version": "0.0.1",
  "main": "build/App.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "build": "npm run clean; npm run tsc",
    "clean": "if [ -d \"build\" ]; then rm -rf build/*; fi",
    "start": "npm run build; node build/App.js",
    "debug": "npm run build; node -r build/App.js.map"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "source-map-support": "^0.5.16",
    "typescript": "^3.8.2"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
